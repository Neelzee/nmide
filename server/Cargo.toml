[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
core = { path = "../core/src-tauri", features = ["server"] }
anyhow = "1.0.93"
tokio = { version = "1.41.1", features = ["full"] }
